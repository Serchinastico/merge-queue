name: 'Main branch CI'
on:
  push:
    branches:
      - main

jobs:
  merge-queue:
    runs-on: ubuntu-latest
    steps:
      - name: MergeQueue
        uses: ./
        with:
          github-token: ${{ secrets.MERGE_QUEUE_GH_TOKEN }}
